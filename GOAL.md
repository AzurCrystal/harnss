# GOAL

## 产品目标

将上游 [Harnss](https://github.com/OpenSource03/harnss) 在保留现有前端框架的基础上完全改造成 oh-my-pi（OMP）专用前端。OMP 与前端通过 OMP 官方 RPC 协议通信；产品不保留其他 agent backend，也不更改 Harnss 当前的任何前端设置。

Harnss 原生 UI 能力必须在实际导入源码后逐项与 OMP 官方 RPC 核对：能够直接表达的接入 OMP，不能直接表达的暂时原样保留；用户明确要求删除的 ACP Agent 功能除外。不得为了完成映射而发明 OMP 官方 RPC 之外的协议、fallback 或抽象。

## 铁律

铁律：只实现当前产品契约所必需的代码。绝对禁止添加未由GOAL,spec或现有失败场景要求的认证、权限、安全审计、对抗性校验、兼容fallback和预防性抽象。保留数据正确性、格式约束、确定性、错误传播以及验收必要的检查。绝对禁止过度兜底和防御性编程。

## 必须范围

- 保留 Harnss 现有前端框架、UI 和前端设置。
- 移除所有非 OMP agent backend，使 OMP 成为唯一 agent backend。
- 移除 ACP Agent 管理、安装、配置、选择及其前端入口。
- 仅按 OMP 官方 RPC 已有契约实现 OMP 通信。
- 从实际 Harnss 源码发现原生 UI 能力，并以源码依据和 OMP 官方 RPC 依据逐项决定“接入 OMP”“暂时保留”或按明确产品契约“删除”。
- 保留数据正确性、格式约束、确定性、错误传播和验收所必需的检查。

## 非目标

- 不升级、替换或重新配置前端框架。
- 不重做 UI，不改变现有前端设置，不新增 OMP 官方 RPC 未提供的产品能力。
- 不提前规定尚未由实际 Harnss 源码、OMP 官方 RPC 或可复现失败场景要求的实现方案。
- 不增加额外认证、权限、安全审计、对抗性校验、兼容 fallback、预防性抽象、过度兜底或防御性编程。

## 完成标准

- 应用仍使用 Harnss 原有前端框架、UI 和前端设置。
- OMP 是唯一参与运行的 agent backend，其他 agent backend 已从运行路径移除。
- 前端通过 OMP 官方 RPC 完成产品所需通信。
- 每项实际发现的 Harnss 原生 UI 能力都有 Harnss 源码依据、OMP RPC 依据和明确处置；可直接表达的能力已接入 OMP，不能直接表达的能力保持原样，明确要求删除的 ACP Agent 功能已完全移除。
- 必要构建检查和真实端到端场景通过，错误按现有契约传播。
- 交付中不存在超出本文件、后续明确 SPEC 或可复现失败场景所要求的代码。

## 变更规则

只有用户明确修改的 GOAL/SPEC 或可复现失败场景才能增加产品要求。核对得到的 OMP 官方 RPC 事实只用于具体化或否定既有能力映射，不能自行增加产品能力；既有要求的实现工作必须在 `PROGRESS.md` 中附依据登记，事实不明时不得猜测、兜底或扩张范围。
